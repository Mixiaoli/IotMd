# IotMd

文档自动化生成项目，解决运维文档滞后问题，开发基于 Markdown 和 Git 的文档管理系统。

## 本次修复说明

针对交互式流程新增了两个关键行为：

1. 当用户选择不先采集交换机信息（`no`）时，系统仍可正常对话。
2. 当采集交换机信息失败时，系统不再抛异常中断，而是进入对话并提示是否稍后重新采集。

对应实现位于 `iotmd/cli.py`，测试位于 `tests/test_cli_flow.py`。
